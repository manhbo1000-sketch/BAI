insert_data.sql
-- ============================================================
-- File: insert_data.sql
-- Bài tập: Sử dụng INSERT INTO để thêm dữ liệu
-- CSDL: QuanLySinhVien
-- Sinh viên: .....................
-- Lớp: .....................
-- ============================================================

USE QuanLySinhVien;

-- ============================================================
-- BƯỚC 1: XÓA DỮ LIỆU CŨ (nếu chạy lại nhiều lần)
-- ============================================================
DELETE FROM Mark;
DELETE FROM Student;
DELETE FROM Subject;
DELETE FROM Class;

-- Reset AUTO_INCREMENT (nếu có)
ALTER TABLE Mark    AUTO_INCREMENT = 1;
ALTER TABLE Student AUTO_INCREMENT = 1;
ALTER TABLE Subject AUTO_INCREMENT = 1;
ALTER TABLE Class   AUTO_INCREMENT = 1;


-- ============================================================
-- BƯỚC 2: THÊM DỮ LIỆU VÀO BẢNG Class
-- ============================================================
INSERT INTO Class (ClassID, ClassName, StartDate, Status)
VALUES (1, 'A1', '2008-12-20', 1);

INSERT INTO Class (ClassID, ClassName, StartDate, Status)
VALUES (2, 'A2', '2008-12-22', 1);

INSERT INTO Class (ClassID, ClassName, StartDate, Status)
VALUES (3, 'B3', CURRENT_DATE, 0);


-- ============================================================
-- BƯỚC 3: THÊM DỮ LIỆU VÀO BẢNG Student
-- Chú ý: Hoa KHÔNG có Phone → để trống (NULL)
-- ============================================================
INSERT INTO Student (StudentName, Address, Phone, Status, ClassID)
VALUES ('Hung', 'Ha Noi', '0912113113', 1, 1);

INSERT INTO Student (StudentName, Address, Status, ClassID)
VALUES ('Hoa', 'Hai phong', 1, 1);

INSERT INTO Student (StudentName, Address, Phone, Status, ClassID)
VALUES ('Manh', 'HCM', '0123123123', 0, 2);


-- ============================================================
-- BƯỚC 4: THÊM NHANH NHIỀU DÒNG VÀO BẢNG Subject
-- ============================================================
INSERT INTO Subject (SubID, SubName, Credit, Status)
VALUES
    (1, 'CF',    5,  1),
    (2, 'C',     6,  1),
    (3, 'HDJ',   5,  1),
    (4, 'RDBMS', 10, 1);


-- ============================================================
-- BƯỚC 5: THÊM DỮ LIỆU VÀO BẢNG Mark
-- ============================================================
INSERT INTO Mark (SubID, StudentID, Mark, ExamTimes)
VALUES
    (1, 1, 8,  1),
    (1, 2, 10, 2),
    (2, 1, 12, 1);


-- ============================================================
-- KIỂM TRA KẾT QUẢ
-- ============================================================
SELECT * FROM Class;
SELECT * FROM Student;
SELECT * FROM Subject;
SELECT * FROM Mark;